<fieldset>
    <legend>{{ localize "op.RollConfiguration.Configuration" }}</legend>
    {{#each fields}}
        {{ formField field name=name value=value options=options input=input }}
    {{/each}}
</fieldset>