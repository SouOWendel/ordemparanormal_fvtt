{
	"group-pull-request-title-pattern": "chore${scope}: release${component} v${version}",
	"release-type": "node",
	"extra-files": [
		{
			"type": "json",
			"path": "system.json",
			"jsonpath": "$.version"
		}
	]
}
